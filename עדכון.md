# סדר עדכון — אחרי כל שינוי

שני צדדים נפרדים, וכמעט תמיד צריך רק את הראשון.

| מה השתנה | מה לעשות |
|---|---|
| קבצים תחת `src/` בלבד | רק שלב א' |
| `google-apps-script/Code.gs` | שלב א' **וגם** שלב ב' |

בסוף כל שינוי ייאמר לך במפורש אילו צדדים נגענו בהם.

---

## שלב א' — האפליקציה

**1.** טרמינל בתיקייה `kehila-crm`:

```
git push
```

**2.** חכה **1-2 דקות**. GitHub בונה את האפליקציה מחדש, וזה לא מיידי.

**3.** באפליקציה: **Ctrl+Shift+R** (רענון עמוק).

**איך יודעים שזה נכנס:** F12 ← Console ← תסתכל על שם הקובץ בשגיאות,
משהו כמו `index-B7EIDELv.js`. **הוא חייב להשתנות** מהפעם הקודמת. אם הוא
זהה — הבנייה עוד לא הסתיימה או שהדפדפן מגיש גרסה שמורה. חכה ורענן שוב.

---

## שלב ב' — הגיליון

רק אם `Code.gs` השתנה.

**1.** פתח את `Code.gs` מהתיקייה `kehila-crm/google-apps-script` והעתק הכל.

**2.** בגיליון ← **תוספים ← Apps Script** ← מחק את כל התוכן ← הדבק ← **שמור**.

**3.** אם נוספה עמודה חדשה — הרץ את `setupSheet` פעם אחת.
בלי זה העמודה לא תיווצר, והפעולה שתלויה בה תחזיר שגיאה.

**4.** **פריסה ← נהל פריסות ← ✏️ עריכה ← גרסה: גרסה חדשה ← פרוס.**

> ⚠️ **לא "פריסה חדשה".** הכפתור הזה מייצר **כתובת חדשה** שהאפליקציה לא
> מכירה, וכל הבקשות יחזירו 404. תמיד "נהל פריסות ← עריכה ← גרסה חדשה" —
> הכתובת נשארת אותה כתובת, רק הקוד מתעדכן.

**איך יודעים שזה נכנס:** בגיליון ← תפריט **לוח בקרה** ← **בדיקת נתונים**.
אם הוא עונה, הפריסה חיה.

---

## סריקה היסטורית מחדש — פעולה חד-פעמית

רק כשתוקן באג שגרם למיילים להיזרק, וצריך למשוך אותם שוב.

**1.** בג'ימייל, חפש:

```
label:"לוח בקרה — נקלט"
```

**2.** בחר הכל ← **הסר תווית**.

הסקריפט מדלג על כל שרשור שנושא את התווית. בלי הצעד הזה הסריקה תדווח
"0 תרומות" ולא תסביר למה.

**3.** בגיליון ← תפריט **לוח בקרה** ← **סריקת כל היסטוריית המיילים**.

**אין סכנת כפילות.** לכל שורה יש מזהה ייחודי — מספר האישור מהספק, או
מספר ההוראה יחד עם החודש — ולפני כל כתיבה נבדק אם הוא כבר קיים.

---

## כשמשהו לא עובד

**"שגיאת חיבור לסנכרון נתונים" / `<!DOCTYPE` בקונסול** — האפליקציה קיבלה דף
HTML במקום נתונים. שלוש סיבות אפשריות, לפי הסדר:

1. **תוסף בדפדפן** ששולח כל בקשה פעמיים ושורף את המפתח החד-פעמי של גוגל.
   בדיקה: פתח את האפליקציה ב**חלון סתר**. אם שם נקי — זה תוסף.
   `chrome://extensions` ← כבה.
2. **הפריסה לא עודכנה** — ראה האזהרה בשלב ב'.
3. **הכתובת באפליקציה שגויה.** בדיקה שלוקחת 10 שניות: הדבק בחלון סתר את
   כתובת ה-`/exec` שלך עם `?action=ping` בסוף. אמור לחזור
   `{"ok":true,"sheet":"..."}`. אם לא — הבעיה בפריסה, לא באפליקציה.

**הכתובת נמצאת ב:** הגדרות ← ערוך פרטי ארגון ← חיבור הנתונים.
שם גם מחליפים אותה — ואז **ממשיכים עד סוף האשף ולוחצים "סיים"**.
"בדוק חיבור" בודק בלבד, הוא לא שומר.
