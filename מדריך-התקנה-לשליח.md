# התקנת המערכת בבית חב״ד נוסף

המטרה: שליח חדש לא נוגע בקוד ולא צריך להבין מהו Apps Script. הוא מקבל גיליון תבנית מוכן ועובר ארבעה צעדים מודרכים.

> המסלול הזה עדיין דורש אישור הרשאות ופריסה חד־פעמית בתוך חשבון גוגל של השליח. גוגל אינה מאפשרת לתבנית פרטית לבצע את שני הצעדים האלה בשמו בלי מערכת מרכזית והרשאות רחבות יותר.

## מה השליח עושה

### 1. יוצר עותק פרטי

פותחים את קישור גיליון התבנית ולוחצים **קובץ ← יצירת עותק**. העותק נשמר ב־Google Drive של השליח, ורק הוא שולט בנתונים שלו.

### 2. מכין את הגיליון

בעותק החדש ממתינים כמה שניות עד שמופיע התפריט **לוח בקרה**. בוחרים **כלים מתקדמים ← התקנה ראשונית**.

גוגל תבקש הרשאות לגיליון ולסריקת הודעות התרומה. מאשרים בחשבון שבו רוצים להפעיל את המערכת. בסיום חוזרים לגיליון ורואים שנוצרו הלשוניות הדרושות.

אם התפריט לא הופיע, מרעננים את הגיליון פעם אחת. רק במקרה שהוא עדיין חסר פותחים **הרחבות ← Apps Script**, בוחרים למעלה את `setupSheet` ולוחצים **הפעלה**.

### 3. מפרסם כתובת חיבור

במסך Apps Script לוחצים **פריסה ← פריסה חדשה** ובוחרים **אפליקציית אינטרנט**:

- לבצע בתור: **אני**.
- למי יש גישה: **כל אחד**.
- לוחצים **פריסה** ומעתיקים את הכתובת שמסתיימת ב־`/exec`.

זו אינה כתובת לעריכת הגיליון. זו כתובת החיבור שהאפליקציה משתמשת בה כדי לקרוא ולשמור נתונים.

### 4. מחבר ובודק

פותחים את האפליקציה, נכנסים למסך החיבור, מדביקים את כתובת ה־`/exec` ולוחצים **בדיקת חיבור**. אם מופיעים שם הגיליון ומספר הגרסה — החיבור תקין.

לאחר החיבור הראשון חוזרים לגיליון ובתפריט **לוח בקרה** מפעילים פעם אחת **סריקת כל היסטוריית המיילים**. מכאן והלאה הסריקה מתבצעת אוטומטית.

## מה אשר מכין פעם אחת לפני שמפיצים

1. ליצור גיליון תבנית נקי, להדביק בו את `Code.gs` המעודכן ולהריץ `setupSheet`.
2. למחוק מהתבנית כל נתון אמיתי: אנשי קשר, תרומות, כתובות, הגדרות ארגון, מפתחות וחיבורי שירותים.
3. לוודא שנשארו רק מבנה הלשוניות, כללי הקליטה והסקריפט המאוגד.
4. ליצור עותק ניסיון בחשבון גוגל אחר ולעבור בו על ארבעת השלבים בדיוק כפי ששליח חדש יעשה.
5. רק אחרי הבדיקה להפיץ קישור מסוג **יצירת עותק**, ולא הרשאת עריכה לתבנית המקורית.

## בדיקת קבלה לפני מסירה

- האפליקציה נפתחת בטלפון ובמחשב.
- בדיקת החיבור מציגה את שם גיליון הניסיון ואת גרסת הקוד הנוכחית.
- הוספת איש קשר, תרומה ומשימה מופיעה לאחר רענון במכשיר השני.
- ניתוק רשת מציג שהשינוי ממתין, וחזרת הרשת שולחת אותו בלי ליצור כפילות.
- אין בתבנית שמות, תרומות, כתובות או הגדרות של בית חב״ד עפולה.

## עדכון עתידי

הפריסה החדשה היא רק בהתקנה הראשונה. בעדכון קוד משתמשים באותה פריסה:

**פריסה ← ניהול פריסות ← עריכה ← גרסה חדשה ← פריסה**.

כך כתובת ה־`/exec` נשארת קבועה והאפליקציה אינה מתנתקת מהגיליון.
