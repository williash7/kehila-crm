# פרסום האפליקציה — קישור לשיתוף

התיקייה הזו כבר מוכנה: יש בה ריפו git עם קומיט ראשון, הבנייה נבדקה ועוברת,
ויש workflow שמפרסם אוטומטית ל-GitHub Pages בכל דחיפה ל-`main`.

נשאר רק לחבר אותה לגיטהאב. **את השלבים האלה צריך להריץ מהמחשב שלך**, כי
פרטי ההזדהות לגיטהאב שמורים שם.

## 1. יצירת ריפו חדש

היכנס ל-<https://github.com/new> וצור ריפו בשם **`kehila-crm`**.

- **Public** (חובה — GitHub Pages בחשבון חינמי עובד רק בריפו ציבורי)
- אל תסמן "Add a README" ואל תוסיף .gitignore — יש כאן כבר.

## 2. דחיפה

פתח טרמינל בתיקייה `kehila-crm` והרץ:

```bash
git remote add origin https://github.com/williash7/kehila-crm.git
git push -u origin main
```

## 3. הפעלת Pages

הדחיפה מפעילה את ה-workflow, שבונה את האפליקציה ודוחף את התוצאה לענף
`gh-pages`. אחרי שהוא מסיים (דקה-שתיים, אפשר לעקוב בלשונית **Actions**):

**Settings ← Pages ← Build and deployment ← Source: Deploy from a branch**
בחר ענף `gh-pages`, תיקייה `/ (root)`, ושמור.

## הקישור לשיתוף

```
https://williash7.github.io/kehila-crm/
```

כל דחיפה ל-`main` מעדכנת אותו אוטומטית.

## מה קורה למי שנכנס לקישור

הוא מקבל את האפליקציה ריקה, ואשף ההגדרה נפתח אצלו. הוא מזין את שם הארגון
שלו, את העיר שלו, ומחבר גיליון Google **משלו**.

**הנתונים שלך לא נמצאים באפליקציה ולא נדחפים לגיטהאב.** הם יושבים בגיליון
הפרטי שלך, והכתובת שלו שמורה רק בדפדפן שלך. אפשר לשלוח את הקישור לכל שליח
בלי לחשוף שום דבר.

## עדכון גרסה בהמשך

```bash
git add -A
git commit -m "תיאור השינוי"
git push
```

## אם משנים את שם הריפו

נתיב הבסיס נגזר אוטומטית משם הריפו (`.github/workflows/deploy.yml`), אז
אין מה לעדכן בקוד — רק לזכור שהקישור משתנה בהתאם.
