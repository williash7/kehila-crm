# רשימת המשך

## 1. תיקון ביצועים ✅ הושלם

`appendByName_` צובר שורות בזיכרון ו-`flushWrites_` כותב אותן בפעולה אחת,
במקום `appendRow` לכל שורה. נוסף `contactIndex_` ו-`hkIndex_` שמונעים
קריאה חוזרת של הגיליון לכל בדיקת קיום.

---

## 2. קליטת הוראות קבע ממייל 🆕

**זו התשובה לשאלה מאיפה מגיעות הוראות הקבע** — ממייל ייעודי של נדרים פלוס
שנשלח כשההוראה מוקמת. עד עכשיו הסקריפט לא ידע עליו בכלל, ולכן לשונית
הוראות הקבע נשארה ריקה.

שולח: `noreply@nedarimplus.com`
זיהוי ייחודי בגוף ההודעה: `להלן פרטי ההוראה שהוקמה`

מבנה השדות:

```
מספר הוראה: 1984716          → מזהה ההוראה
שם תורם: כהן מאור בנימין      → שם
סכום כל חיוב: 250.00 ש"ח      → סכום
תאריך חיוב הבא: 02/11/2025    → תאריך פתיחה
תדירות גביה: חודשי
מס' חיובים: 12                → מספר תשלומים
קטגוריה: חבד בעליה            → קמפיין
מייל: maorcohen7@gmail.com
טלפון / כתובת / עיר
```

להוסיף כלל שלישי ל-`seedNedarimRules_` עם `סוג = standingOrder`, שכותב
ללשונית `הוראות קבע` לפי `מספר הוראה` כמזהה (ולכן אידמפוטנטי).
שאילתת חיפוש מוצעת: `from:noreply@nedarimplus.com "להלן פרטי ההוראה שהוקמה"`
— לא תלויה בשורת הנושא.

---

## 3. תרומה בתשלומים — להסיר את הדילוג

דוגמה אמיתית (מייל "העסקה שנתקבלה"):

```
תאריך עסקה: 06/10/2025 09:45
שם: בואהרון שרה
סכום: 1800.00 ₪
תשלומים: 12
מספר אישור: 0119185
קטגוריה: חבד בעליה
```

זו **לא** הוראת קבע — זו תרומה חד-פעמית של ₪1,800 שנגבית ב-12 תשלומים.

כרגע `handleDonationEmail_` מדלג על כל מייל עם `תשלומים > 1`, ולכן ₪1,800
נעלמים מהדוחות.

### המסקנה: הדילוג נכון — להשאיר אותו

בדיקה מול הנתונים האמיתיים (גיליון `נתוני קמפיין`, 123 עסקאות):

- 84 עסקאות בתשלום אחד — **כולן** `אמצעי תשלום = רגיל`
- 39 עסקאות בתשלומים — **כולן** `אמצעי תשלום = הוק`, ולכולן יש הוראת קבע מקבילה

ואימות נקודתי: המייל של "בואהרון שרה · ₪1,800 · 12 תשלומים" מתאים לשורת
`שי בוארון · 12 תשלומים · ₪150 · הוק` בגיליון הקמפיין. 150 × 12 = 1,800 —
אותה עסקה בדיוק, בשני ייצוגים.

כלומר מייל "העסקה שנתקבלה" עם תשלומים מדווח על **הסכום הכולל** של הוראת
קבע שכבר מדווחת גם במייל "ההוראה שהוקמה". רישום הסכום המלא + ייצור החיובים
החודשיים = ספירה כפולה של אותו כסף.

בנוסף, הכסף עדיין לא התקבל בפועל — הוא נכנס חודש בחודשו, וחיוב שנכשל מייצר
מייל סירוב שכבר מטופל.

**מה כן לעשות:** להשאיר את `if (payments > 1) return false;`, אבל לא לדלג
בשקט — לרשום לוג/התראה על עסקה בתשלומים שאין לה הוראת קבע תואמת (לפי שם
וסכום חודשי). בנתונים הקיימים אין אף מקרה כזה, אבל אם יופיע הוא לא ייעלם.

---

## 4. סנכרון בין מכשירים — התחברות עם גוגל

היום הגדרות הארגון יושבות ב-localStorage, ולכן במכשיר חדש צריך להריץ את
האשף שוב. הנתונים עצמם כבר מסונכרנים דרך הגיליון.

מה שהמשתמש ביקש: **כניסה עם גוגל** ואז הכל נטען לבד.

שני מסלולים:
- **קטן ומיידי:** לשמור את הגדרות הארגון בלשונית `סנכרון נתונים`. במכשיר חדש
  מדביקים רק את כתובת הגיליון והשאר נטען לבד.
- **מלא:** Google Sign-In באפליקציה + איתור הגיליון אוטומטית מה-Drive של
  המשתמש. בלי הדבקת כתובות בכלל, אבל דורש הגדרת OAuth Client.

---

## 5. תיקון הוראות ההתקנה באשף 🔴

**ממשק Apps Script מוצג בעברית** (אומת בצילום מסך), ולכן ההוראות באנגלית
שכתובות היום ב-`SetupWizard.tsx` פשוט לא תואמות למה שרואים על המסך.

הניסוח הנכון:

| מה שכתוב היום | מה שצריך |
|---|---|
| Deploy ← New deployment | **פריסה ← פריסה חדשה** |
| Web app | בחירת סוג: **אפליקציית אינטרנט** |
| Execute as: Me | לבצע בתור: **עצמי** |
| Who has access: Anyone | למי יש גישה: **כולם** |
| — | לסיום: **לפריסה** |

להשאיר את האנגלית בסוגריים למי שהממשק שלו באנגלית.
**וחסר לגמרי:** מסך אישור ההרשאות של גוגל — כולל האזהרה
"האפליקציה לא מאומתת" והצורך ללחוץ "מתקדם ← המשך".

---

## 6. כרטיסי ייבוא עם AI

עוד לא נבנה. שלושה כרטיסים: אנשי קשר ותרומות · דוגמאות מיילים · לוז קבוע.
כל אחד מייצר פרומפט להעתקה, והמשתמש מדביק את הפלט בחזרה.
חובה מסך אישור עם ספירה ודוגמה לפני שמירה, וזיהוי JSON שנקטע באמצע.
